// import { setCookie } from 'cookies-next'
// import { createSlice } from '@reduxjs/toolkit';

// const setAuthSession = (state, action) => {
//   const { user, token } = action.payload;
//   state.user = user;
//   state.token = token;
//   setCookie(null, 'token', token, {
//     maxAge: 30 * 24 * 60 * 60,
//     path: '/',
//   })
// }